//2. Given an array of numbers, padding amount and repeat count. 
// Pad the array in the following way: the padding amount specifies 
// how many elements should be taken from the array edges, the repeat
// amount specifies how many times the pad should be repeated.
// Also, you should check that  padding amount <= length of array.
